"use client";import {Canvas,useFrame} from "@react-three/fiber";import {Points,PointMaterial} from "@react-three/drei";import {useMemo,useRef} from "react";import * as THREE from "three";function Network(){const ref=useRef<THREE.Points>(null);const positions=useMemo(()=>{const a=[];for(let i=0;i<240;i++){const r=2.1*Math.sqrt(Math.random()),t=Math.random()*Math.PI*2;a.push(Math.cos(t)*r,(Math.random()-.5)*2.8,Math.sin(t)*r)}return new Float32Array(a)},[]);useFrame((s)=>{if(ref.current)ref.current.rotation.y=s.clock.elapsedTime*.08});return <Points ref={ref} positions={positions} stride={3}><PointMaterial color="#b8ff3d" size={.025} transparent opacity={.8}/></Points>};export default function SkillScene(){return <Canvas camera={{position:[0,0,5]}} dpr={[1,1.4]}><ambientLight intensity={.4}/><Network/></Canvas>}